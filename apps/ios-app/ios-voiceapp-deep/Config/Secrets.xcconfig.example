// Copy to Secrets.xcconfig in this folder (gitignored). Included last from Release.xcconfig — overrides Config/Shared.xcconfig.

VOICE_TOKEN_BASE_URL = https:/$()/your-production-host.example.com/token
